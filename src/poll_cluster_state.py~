        role,ip = info
                if role == 'Master':
                                continue
                                    ips.append(ip)
                                        return ips~
